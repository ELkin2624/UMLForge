/**
 * VoiceAssistantChat Component Entry Point
 * Refactored to follow Feature-Sliced Design (FSD) and Single Responsibility Principle (SRP).
 * Canonical implementation now lives in `src/features/voice`.
 */
export { VoiceAssistantChat } from '../features/voice';
