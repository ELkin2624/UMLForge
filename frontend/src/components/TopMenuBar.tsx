/**
 * TopMenuBar Component Entry Point
 * Refactored to follow Feature-Sliced Design (FSD) and Single Responsibility Principle (SRP).
 * Canonical implementation now lives in `src/widgets/top-menu-bar`.
 */
export { TopMenuBar } from '../widgets/top-menu-bar';
export type { TopMenuBarProps } from '../widgets/top-menu-bar';
